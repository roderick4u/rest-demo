spring:
  datasource:
    url: jdbc:mysql://localhost:3306/rest_demo_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: rest_user
    password: viajero123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect